server:
  port: 0

grpc:
  server:
    port: 0

collector:
  kafka:
    bootstrap-servers: localhost:9092
    topics:
      user-actions: stats.user-actions.v1

logging:
  level:
    ru.practicum: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info
